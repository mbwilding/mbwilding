name: GitHub Tiles

on:
  workflow_dispatch:
  schedule:
    - cron: "0 11 * * *"

jobs:
  ["GitHub Tiles"]:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: mbwilding/github-tiles@main
